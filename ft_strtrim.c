#include "libft.h"

static size_t check_char(char const c, char  *s1)
{
	while (*s1)
	{
		if (c == *s1)
			return (1);
		s1++;
	}
	return (0);
}

char *ft_strtrim(char const *s1, char const *set)
{
  
}

int main(void)
{
  
}